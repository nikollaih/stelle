import Constants from '@/Util/Constants.json';
export const styles = {
    container: {
        background: `linear-gradient(to bottom, ${Constants.COLORS.PRIMARY}, ${Constants.COLORS.SECONDARY})`
    },
};
